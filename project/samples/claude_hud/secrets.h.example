// 복사해서 secrets.h 로 저장하고 실제 값을 채우세요. (secrets.h 는 git 에 올라가지 않음)
// 여러 WiFi 를 넣으면 켜져있는 곳(신호 좋은 곳)에 자동 접속됩니다 (WiFiMulti).
#pragma once

#define WIFI_SSID1 "HOME_SSID"
#define WIFI_PASS1 "HOME_PASSWORD"

#define WIFI_SSID2 "OFFICE_SSID"
#define WIFI_PASS2 "OFFICE_PASSWORD"

// 필요하면 3번째도 (주석 해제)
//#define WIFI_SSID3 "..."
//#define WIFI_PASS3 "..."
